{
  "name": "randomcharts",
  "devDependencies": {
    "jshint": "latest",
    "browserify": "latest",
    "chai": "latest",
    "mocha": "latest",
    "mocha-phantomjs": "latest"
  },
  "dependencies": {
    "seedrandom": "latest",
    "scriber-js": "latest",
    "babel-cli": "latest",
    "babel-preset-es2015": "latest"
  },
  "scripts": {
    "clean": "rm -rf lib",
    "lint": "jshint src || true",
    "babel": "babel src -d lib",
    "test-only": "mocha-phantomjs SpecRunner.html",
    "test": "npm run clean & npm run babel & npm run test-only",
    "build": "npm run clean & npm run lint & npm run babel & npm run test"
  }
}